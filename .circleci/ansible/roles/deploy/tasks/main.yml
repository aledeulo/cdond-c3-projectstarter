---

- name: "Creates directory"
  file:
    path: ~/web
    state: directory

- name: "Extract files"
  unarchive:
    src: artifact.tar.gz
    dest: ~/web

- name: "Executing node"
  shell: npm install ~/web

- name: "Run backend"
  shell: cd ~/web && pm2 start npm -- start